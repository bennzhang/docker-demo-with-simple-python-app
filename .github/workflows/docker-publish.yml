name: Docker

on:

  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: push docker image
        uses: mr-smithers-excellent/docker-build-push@v6
        with:
          image: oussamalakhdar/python-github-actions
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_PASSWORD }}


# name: Build and Push Docker Images

# on:
#   push:
#     branches: [ "master" ]
#   pull_request:
#     branches: [ "master" ]

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v2

#     - name: Login to Docker Hub
#       uses: docker/login-action@v1
#       with:
#         username: ${{ secrets.DOCKER_USERNAME }}
#         password: ${{ secrets.DOCKER_PASSWORD }}

#     - name: Build and push HR service
#       run: |
#         docker build -t olakhdar/python-github-actions .
#         docker push olakhdar/python-github-actions

    # - name: Build and push HR service
    #   run: |
    #     docker build -t your-dockerhub-username/hr-service ./hr-service
    #     docker push your-dockerhub-username/hr-service

    # - name: Build and push AUTH service
    #   run: |
    #     docker build -t your-dockerhub-username/auth-service ./auth-service
    #     docker push your-dockerhub-username/auth-service

    # - name: Build and push FINANCE service
    #   run: |
    #     docker build -t your-dockerhub-username/finance-service ./finance-service
    #     docker push your-dockerhub-username/finance-service
      
